Error Boundary 能捕获什么？
可以捕获：
	• 渲染阶段的错误
	• 生命周期中的错误
	• 子组件树中抛出的错误
不能捕获：
	• 事件处理函数（onClick）
	• 异步代码（setTimeout、Promise）
	• 服务端渲染
	• 自己 Error Boundary 内的错误

✅ 标准写法（最常用）

class ErrorBoundary extends React.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false };
  }
static getDerivedStateFromError(error) {
    return { hasError: true };
  }
componentDidCatch(error, info) {
    console.error("Caught by ErrorBoundary:", error, info);
  }
render() {
    if (this.state.hasError) {
      return <h2>页面出错了，请稍后重试。</h2>;
    }
    return this.props.children;
  }
}
使用方式：

<ErrorBoundary>
  <YourComponent />
</ErrorBoundary>

📌 捕获异常流程
	1. 子组件渲染时抛出错误
	2. 进入 getDerivedStateFromError，设置 hasError
	3. 渲染 fallback UI
	4. componentDidCatch 记录日志（推荐上报到监控系统）

📌 简单示例

function Bug() {
  throw new Error("Something went wrong!");
}
function App() {
  return (
    <ErrorBoundary>
      <Bug />
    </ErrorBoundary>
  );
}
结果：显示 fallback UI，不会导致整个应用白屏。



📌 Hooks 时代怎么办？能用函数组件吗？
目前：
❌ 不能使用函数组件直接实现 Error Boundary
✔ 但 React 团队未来会提供函数版错误边界（React 官方 roadmap）
现在可做的是：
	• 自己写 Class 组件（上面示例）
	• 或使用第三方库（如 react-error-boundary）

🔥 推荐：函数组件项目最好的方案（第三方库）

npm i react-error-boundary
示例：

import { ErrorBoundary } from "react-error-boundary";
function Fallback({ error }) {
  return <p>出错了：{error.message}</p>;
}
<ErrorBoundary FallbackComponent={Fallback}>
  <YourComponent />
</ErrorBoundary>
✔ 支持函数组件
✔ 更灵活，推荐在实际项目中使用

⭐ 总结一句话（面试用）
	React 的异常捕获组件叫 Error Boundary，
	当前只能用类组件实现，用 componentDidCatch 和 getDerivedStateFromError 捕获渲染错误，
用于防止整个应用崩溃。
